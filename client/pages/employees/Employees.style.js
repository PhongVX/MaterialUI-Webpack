const employeesStyle = (theme) => ({
    buttonAdd:{
        margin:20,
        float:"right"
   },
   root: {
    width: '100%',
    marginTop: theme.spacing(3),
    overflowX: 'auto',
  },
  table: {
    minWidth: 650,
  }
})

export default employeesStyle